# 全台系まつわるチェッカー やること
最終更新 2026-08-29

確認シート: https://claude.ai/code/artifact/9a7aa3af-8b7a-4760-b70e-47083aac3e31
（イベント傾向が実際に使っている分類。設定ファイルからそのまま書き出している）

## 直したら必ず push する

**ローカルでコミットしてデプロイしただけでは、次の自動更新に消される。**
毎日の更新は GitHub のリポジトリからコードを取り出して動くので、push していない
変更は「無かったこと」として本番が作り直される。2026-08-29 にこれが起きて、
タブ・全機種シート・未実施の改修がまとめて巻き戻った（24コミットが未pushだった）。

    cd /Users/Shared/kabaneri-counter/github-pages
    git add -A && git commit -m "..." && git push origin main
    npx wrangler pages deploy . --project-name=kabaneri-counter --branch=main

自動更新が先に走っていると push が弾かれる。そのときは `git fetch` してから
`git merge origin/main` し、`toho/data/events.json` の衝突は生成物なので
`git checkout --ours` で自分側を採用し、`events.py` と `src/build-pages.py` を
流し直してからコミットする。


## 分析するときに忘れないこと

**バラも全系に含める（2026-08-28にユーザー確認済み）。**
記事タイトルは「全系6機種+バラ3機種」のように分けて書かれ、結果欄（台数つき）に
載るのが「全系」、連想の見出しにだけ出るのが「バラ」。全体の15.7%（189件）が後者で、
その68%が1台構成。**これらも全系として数えるのが正しい。**
結果欄だけに絞ろうとしないこと。


**結果欄には全台系でないものが混ざる。色付きは機種イベントの一覧表。**
「かぐや様（ピンク）」「モンキーV（青）」のような色付きの行は、4のつく日の
機種イベント対象台を示す一覧であって、その日の全台系ではない。
6/04・6/14・7/04の3日にこれが載っており、括弧書きを外す処理を入れたときに
全台系として数えてしまっていた（2026-08-28にユーザーが発見）。
`events.py` の `COLOR_TAG` で除外している。台数としては使う。
**タイトルに「◯機種が全系に！」と書いてあるので、件数が合うかで検算できる。**


**単独の軸だけ見ると傾向を見逃す。掛け合わせを見ること。**
小台数（4〜9台）のジャグラーがその実例。3のつく日で
- ジャグラー系統だけ見ると 8/12回（全体74%）で**平均以下**
- 小台数だけ見ると 11/12回（全体94%）で**差なし**
- 掛け合わせると 8/12回（全体54%）で **+13**
どちらの軸も「当たり前に来るもの」なので単独では埋もれる。
島として意味のあるまとまり（ジャグラーコーナーの5台6台、など）は
軸を分解すると消えてしまう。

**小台数ジャグラーは実在する強い傾向なので、今後も必ず見ること。**
現在の出方: 7のつく日 10/12回、22日・24日・27日 6/7回、あかまる取材 4/5回、
3のつく日・5のつく日 8/12回、月末最終日 4/6回。
`events.py` の `smalljug_share()` で集計、`src/events.html` の系統行に表示。

未実施: 系統×台数帯の総当たり（小台数のハナハナ、多台数のジャグラーなど）で
ほかに見落としがないか洗う。


**開催回数が少ない日は「100%」に意味がない。必ず全体と比べること。**
ママ誕生日（6回）で「小台数4〜9台と3台以下がどちらも6/6回＝毎回入っている」を
特徴として書いてしまったが、全体でも94%・90%あり、6回続くのは7割方起きる。
ユーザーに指摘されて撤回した（2026-08-28）。
差を見ずに割合だけ見ると、当たり前のことを傾向として書いてしまう。

## 残っていること

### B. まつわり数字（34機種ぶん確認済み・以後は追加分だけ）
2026-08-28に全機種の判定を確認済み。今後は新しい機種が入ったときに見ればよい。
`toho/data/readings.json` の「まつわり数字」「まつわり除外読み」で調整する。
- 正式名称に数字が隠れている機種は読みを足せば効く
  （エウレカ＝エウレカセブン＝7 がこのパターン）
- 機械的に読むと間違えるものは「まつわり数字」に手で書く
  （ハッピー＝3。ハッピージャグラーVⅢのVはシリーズ名で5ではない）
- まつわりなしと決めたものは空配列にする（1000ちゃん）

### F. ママ誕生日（2日）の大都を、回数が増えてから見直す
ユーザーは前職からこのイベントを担当していて、**特徴の薄いイベントという認識**。
現状 大都 5/6回（83%・全体65%）で唯一の手がかりだが、6回しかなく確率のブレの
範囲を出ない。リードは「大きい島（10台以上）は1機種。あとは小台数から」のまま
維持し、大都は結論でだけ触れている。
**回を重ねても大都が優勢なら、そのときリードに入れる**（2026-08-28にユーザーと合意）。

### C. ブログ運営者への許諾連絡（未送）
下書き: `/Users/Shared/matsuwaru-permission-draft.md`

### D. minnanoslot.com のトップページから /matsuwaru/ へのリンクが未設置

### E.（解決済み）自動更新の遅延 → Cloudflare から叩く形にした
GitHub Actions の定時実行は**10〜12時間ずれていた**（09:17設定が19:43に実行など）。
分をずらしても効かず、混雑時に後回しにされるのが原因。手動実行は指定どおり動くので、
Cloudflare Workers の定時実行から GitHub の手動実行APIを叩く形に変えた。

- Worker: `/Users/Shared/kabaneri-counter/dispatch-worker/`（リポジトリ外・未コミット）
- 起動時刻: 08:10 / 09:10 / 10:10 / 13:10 JST
- GitHub のトークンは Cloudflare のシークレット `GH_TOKEN`。
  ユーザーが登録済みで、こちら（Claude）は中身を受け取っていない。
  権限は Actions=Read and write のみ（Metadata は自動で付く）
- 状態の確認: `curl -s https://matsuwaru-dispatch.kyon0520.workers.dev/`
- 2026-08-29 10:20 に実際に発火することを確認済み
- GitHub 側の cron（09:17/10:37/13:23 JST）は保険としてそのまま残してある

## 済んだこと（2026-08-28）

- **バラエティ機種（1台構成）を集計に追加したが、傾向は無かった**。
  全体9%に対し、開催5回以上のイベントは4〜11%の範囲に収まる。
  大都の日（0のつく日）+2、ノーマルの日（5のつく日）+1 で、いずれも誤差の範囲。
  データは events.json に持っているが、8ポイント以上離れた日だけ文章に出す設定なので
  現状はどのイベントでも表示されない。おまけの島という位置づけがそのまま出た形。

- **自動更新が止まっていた**。GitHub Actions のスケジュールは毎時00分に置くと
  発火しないことがある。分をずらして3回に増やした。
- **台数の出どころを日付の近い順に**。ブログに台数が無い記事があるため、
  マルハン公式（取得日つき）と比べて近いほうを使う。当日記載79.5%。
- **メーカーを全機種ぶん調査**（20メーカー）。
- **種別（ノーマル31／AT78）を確定**。ユーザー確認済み。
- **数字まつわりを実装**。機種名・読み・台数の末尾で日付の数字を見る。
  7のつく日は12/12回で成立。この店の本質的なロジック。
- 機種の整理：ドッチ→アニマルドッチに統合、「アイム・東リベ」を2機種に分割、
  ギアス→ギアスC.C.、沖ドキDUOアンコールを公式の「オキドキデュオ2」と照合。
- 集計ルール：不定期イベントは置き換え、21-27WEEK・月末最終日は◯のつく日に数えない、
  にゃっぴーは12日のみ、ゾロ目は11日のみ、やばたにえんは23日に統合。
