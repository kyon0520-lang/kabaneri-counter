# やること

2026-08-19 時点。着手するときはここを見る。

いまの公開状態は README の「トップページ」「送信機能の入り切り」を参照。
要約すると、**カウンターは送信なし（保存のみ）で公開中、トップページは準備中の画面**。
シェアはまだしていない。

---

## すぐ着手できるもの

### 1. ホーム画面への追加を促す案内 — **2026-08-19 実装済み**

実装したもの: iPhone（iOS）の**ブラウザで初めて開いたときだけ**、全画面の案内を出す。
`kabaneri-a2hs-v1` を localStorage に置いて、以降は出さない。
「わかりました」でも右上の×でも閉じられ、閉じたあとは普通に使える。
すでに保存済みの記録がある人には「記録の引き継ぎ」への案内が1行増える。
Safari以外のiOSブラウザ（Chrome等）には「Safariで開き直す」手順を出す。

**決めたこと（当時の未決分）**:
- **Androidには出さない**。Chromeはブラウザとインストール後のPWAで
  localStorage を共有するので、この分裂が起きない。実害が無いのに全画面は挟まない
- **PCにも出ない**（iOS判定に入らないため、幅の判定は不要だった）
- **出すのは初回だけ**。数日後の再表示は入れていない

**やらなかったこと**: ワンタッチで追加するボタン（2026-08-19 に不採用）。
iOS Safari は `beforeinstallprompt` を実装しておらず、追加をプログラムから
呼ぶ手段が無い。Androidなら本物のワンタッチが作れるが、そもそも案内を出さない
方針にしたので作らない。

**残っている案**: 保存ボタンを押した瞬間にも一度だけ警告する案（b）。
実害が出る直前に知らせるので効果が高い。未実装。

<details><summary>当時の設計メモ</summary>

**なぜ**: iOSでは Safari とホーム画面アプリで localStorage が完全に別物になる。
Safariで保存した実戦は、あとからホーム画面に追加したアプリからは1件も見えない。
同じ端末・同じURLなので、利用者はまず気づけない。シェア後に「記録が消えた」と
言われる可能性が高い。

**やらないと決めたこと**: ブラウザ利用そのものを塞ぐ案は不採用。
iOSではホーム画面に追加するのに一度Safariで開く必要があり、唯一の入口を塞ぐことになる。
中身を見る前に手間を要求すると離脱する。

**やること**: Safariで初めて開いたときだけ、全画面で追加手順を出す（閉じられる）。
- 判定は `display-mode: standalone` と `navigator.standalone`
- 理由を先に、手順（共有ボタン→ホーム画面に追加→アイコンから開く）を後に
- 「このまま使う」の逃げ道を残す
- すでに保存済みの人向けに「記録の引き継ぎ」への導線を一行

**決めていない論点**:
- Androidのブラウザでも出るが、文言は「ホーム画面に追加」でよいか（Chromeは「インストール」）
- PCでも出る。画面幅が広いときは出さないか
- 出す回数は初回だけか、数日後にもう一度だけか

**あわせて**: 保存ボタンを押した瞬間にも一度だけ警告する案（b）がある。
実害が出る直前に知らせるので効果が高い。aと組み合わせてもよい。
</details>

### 2. 取扱説明書に、Safariとホーム画面アプリで記録が別になることを書く

8章「データの保存について」には「別の端末やブラウザには引き継がれません」としか書いていない。
利用者は「同じiPhoneの同じサイト」だと思っているので、これでは伝わらない。

### 3. 設定5・6の実データを取り込む口

判明している設定の実戦データは判別の土台として最も価値が高いが、いまメモにしかない。
カウンターは打ちながら押す前提なので、あとから入れるには非発光を何百回もタップすることになる。

管理者ページに取り込みフォームを作り、`/api/records` と同じ経路で保存する。
取り込んだ分は `app_version` を `import` などにして、打ちながら数えた記録と区別できるようにする。

**確認が必要**: メモにCZ突入時のポイントが1回ずつ残っているか。
残っていれば規定pt分布まで作れる。合計しかないなら発光率と総ゲーム数だけになる。

### 4. claude.ai アーティファクト版（`src/share.html`）の引っ越し画面（設計まで済み・保留）

いまはカウンターがそのまま動き、途中に移行バナーが出ているだけ。
これを丸ごと引っ越し案内の1画面に差し替えて、数えられないようにする案。
**やるかどうかは未決。** 画面案は 2026-08-13 の会話で確定させたところまで。

**入れる内容**（デモ済み）:
- 「専用サイトに引っ越しました」＋ 新URL ＋「新しいサイトを開く」ボタン
- 新しいほうで増えた機能を、青いチェック＋14pxの太字で6行
  （当選履歴カウンター／周期当選率・ST確率／実戦履歴の保存機能／メモ機能／
  オフライン起動／記録の引き継ぎ）
- 「記録は移せません」を黄色の枠で独立させる。
  claude.ai と minnanoslot.com は別サイトなので localStorage も引き継ぎコードもまたげない

**注意**:
- 差し替えた瞬間、数えている途中の人のカウントは消える
- アーティファクトではリンクを押すと claude.ai の確認ダイアログが一度挟まる（仕様）
- 表記は「実**戦**」で統一（アプリと取説がすべて実戦のため）
- 更新は Artifact ツールで既存URLを指定して上書きする。
  https://claude.ai/code/artifact/46135a9b-81bc-4047-9991-0cc9222785ba

**未決**: 結果カードを一覧に載せるか（いまは「実戦履歴の保存機能」に含めた扱い）。
取扱説明書へのリンクを残すか。

---

## 保存の会員制化 — **2026-08-26 本番稼働中**

本番（minnanoslot.com）で送信機能を有効にした（sw v106）。
`send.minnanoslot.com` を Resend で認証済み、差出人は `login@send.minnanoslot.com`。
`info@minnanoslot.com` は Cloudflare Email Routing でGmailへ転送。

**できたもの**:
- 数えるのは登録不要、保存だけ参加（メール登録）が要る形
- マジックリンク（パスワードなし）。`/api/auth/request` → メール → `/api/auth/verify` → HttpOnly Cookie
- メニューに「実戦履歴を読み出す」（`/api/myrecords`）。端末が変わっても同じ記録を読み出せる
- 「アカウントと記録をすべて削除」（`/api/account/delete`）
- `privacy.html`
- メモは預かるが集計には使わない（参加画面とポリシーに明記）
- Resend 連携。実機でメール受信を確認済み

**残っている作業**:
- **取扱説明書に、参加・読み出し・削除の説明を足す（未着手）**。
  いまの取説は「保存は登録不要」の前提のまま。実際の動きと食い違っている
- DMARC（`_dmarc` に `v=DMARC1; p=none;`）は未設定。無くても動くが、
  ドメインを騙るメールを受信側が弾きやすくなる
- `www.minnanoslot.com` に到達できない（CloudflareのRecommendationsに出ている）

**詰まったところ（次に同じことをするとき用）**:
- **Pagesの環境変数・シークレットは、入れただけでは効かない。** 反映には
  再デプロイが要る。キーを入れ直しても401が続いたのはこれが原因だった
- **独自ドメイン側では、5xxで返すと本文がCloudflareのエラーページに差し替わる。**
  送信失敗の理由が読めず原因を追えなかったので、業務上の失敗は200で
  `ok:false` を返す形にした

**マイページ（`/mypage/`）— 2026-08-21 実装済み**
ブラウザでログインして、預けた実戦を見るページ。アプリを開かずに見られる。
全実戦を通した発光率・平均pt/CZ・周期当選率・規定pt分布を出し、
発光率と平均pt/CZ には「みんなの中央値との差」を併記する。
周期当選率の数え方はカウンターと同じ（`doneCycles`）。ずれると別物になるので、
片方を直したらもう片方も直すこと。

**将来: 中央値からの設定判別（未着手）**
マイページに「あなた ／ みんな」の並びをすでに置いてあるので、その延長になる。
必要なのは設定別の基準値で、いまは無い。作る順番は
(1) 設定申告つきの記録を貯める → (2) 設定別の分布を出す → (3) 判別を当てる。
**(1) が足りないうちに (3) を出してはいけない。** 外れた判別を先に見せると、
サイトの信頼を最初に壊す。件数のしきい値と、外れうることの明示は必須。

<details><summary>決定時のメモ（2026-08-13）</summary>

**いちばんの動機**: iOSでSafariとホーム画面アプリのデータが分かれる問題。
端末の中に置いている限り、この分裂は原理的に解決できない。
サーバーに置けば消える。ローンチの障害になっていたのはこれ。

### 決めたこと

**数えるのは登録不要。保存だけ登録制。**
ホールで開いてすぐ数える体験は無傷のまま、
「記録を残したい」と本人が思った瞬間にだけ登録を求める。

**登録方式はメールアドレスのマジックリンク**（パスワードなし）。
- ログインも復旧も「メールを入れて届いたリンクを押す」だけ
- **パスワードを忘れる事故が構造的に起きない**
- パスワード方式はマジックリンクの上位互換ではない。「忘れた」の対応で
  結局メール送信が要るので、マジックリンクを作った上にパスワード管理を足す形になる
- Googleログインはメール基盤が不要で楽だが、パチスロのツールに
  Googleアカウントを繋ぐ抵抗が一定数ある

**バックアップコード方式は不採用**。紛失に打つ手がなく、
増えるほど「戻せません」と言い続けることになる。

**保存する人には、みんなの集計への提供も同意してもらう**（バンドル）。
「みんなで持ち寄る」が看板なので筋が通る。数えるだけの人には一切求めない。

**18歳以上の確認は入れない**（2026-08-13 判断）。

### 外せない設計

- **保存はまずローカル、そのあと同期**。ホールは圏外がある。
  「保存を押す→登録画面→メールが届かない」で数えた成果が消えるのは最悪。
  端末に保存してから、登録と同期を促す
- **メモは保管するが提供には含めない**。復元のためサーバーに置く必要はあるが、
  店舗名など一番センシティブな部分。同意文にもデータ設計にも明記する。
  利用者の選択は1つでも、内部では「保管する項目」と「提供する項目」を分ける
- **同意文は将来やることまで含む広さで書く**。一度もらった同意で用途は増やせない。
  含めるもの: 統計として集計／設定判別に使う／サイト上で統計値として公開。
  書かないもの: 第三者提供・販売（やるときに改めて取る）
- **「アカウントと記録をすべて削除」を最初から用意する**。
  法律の要求というより、苦情を苦情のまま終わらせないための装置
- **コードで束ねると、その人の全記録が1つのIDに紐づく**。
  「誰がどれだけ送ったか」は公開しない（既存の開示範囲の表と同じ扱い）

### 実務で必要になるもの

- **メール送信サービス**。Cloudflare単体では送れない。
  MailChannels の無料枠は **2024年6〜8月で終了**。いまCloudflareが公式に勧めているのは
  **Resend**（月3,000通まで無料）。マジックリンクは1ログイン1通なので当分足りる
- Resend のアカウントと、**minnanoslot.com の送信認証（SPF/DKIM）のDNS設定**。
  ドメインはCloudflare管理下なので設定は簡単
- **プライバシーポリシー**。メールアドレスを保存した時点で個人情報を扱うことになる。
  利用目的の明示と削除の窓口。1ページ置けば実務上は足りる

### サーバー保管そのものの利点（会員制の副産物ではなく本命）

- **判別ロジックは必ず後から変わる**。刻みの粗さ、銅藍の扱いなど。
  生データがサーバーにあれば**過去分すべてに遡って計算し直せる**。
  端末内にしかないと、新しく届いた分にしか新しい集計方法を適用できない。
  最初の数百件を作り直せるかどうかは、判別の精度に直結する
- 同意した瞬間に**過去分もまとめて集計対象にできる**
- 「あなたの平均 / みんなの平均」の比較が自然に書ける
- 同じ人の記録を束ねられる。端末IDだけだと機種変更のたびに別人になる

### 実装の順番（案）

1. D1にユーザーと記録のテーブルを足す（`schema.sql`）
2. マジックリンクの発行と検証（Resend連携、トークン、セッション）
3. 保存時の同期（ローカル保存が先、失敗しても数えた分は残す）
4. ログイン済みの端末で記録を復元する
5. アカウントと記録の削除
6. プライバシーポリシーのページ
</details>

---

## みんなの集計まわり（順番が決まっているもの）

疎通確認は済み。サーバーは正常に受けて保存するところまで動作確認した（テスト行は削除済み）。
D1 はいま0件。

### 着手する順番

0. **送信を公開する前に、読み取りの作りを直す**（下記。これが先）
1. **`/api/stats` の拡張** — いまは件数・中央値・分布のみ。端末数と、
   件数がしきい値未満なら数字を伏せる判定を足す
2. **トップページに件数を出す** — 0件のうちは数字を出さず「準備中」
3. **管理者ページ `/admin`** — 認証は管理キー1本をCloudflareのシークレットに置き、
   サーバー側で照合する方式（Cloudflare Access は将来の選択肢）。キーはURLに載せない
4. **アプリ内の「みんなの集計」タブ**

### 0. 送信を公開する前に直すこと（2026-08-19 に判明・未着手）

**症状**: 保存できる件数は容量ではなく、**D1の1日の読み取り上限で先に頭打ちになる**。

`functions/api/records.js` の POST が、送信1件ごとに次を実行している。

1. `SELECT COUNT(*) FROM records WHERE machine = ?`（全件走査）
2. `median(avg_pt)` — `avg_pt` に索引が無いので全走査＋並べ替え。中の副問い合わせで
   もう一度カウントするため、実質2回走る
3. `median(glow_rate)` — 同上

D1 は**走査した行数**を読み取りとして数えるので、1送信あたり
**保存済み件数のおよそ5倍**の行を読む。件数が増えるほど1送信が重くなる。

| 保存済み件数 | 1送信あたりの読み取り | 無料枠(500万行/日)で通る送信数 |
|---|---|---|
| 1,000 | 5,000行 | 1,000件/日 |
| 10,000 | 50,000行 | 100件/日 |
| 50,000 | 250,000行 | **20件/日** |
| 150,000 | 750,000行 | **6件/日** |

**2〜3万件あたりで、1日数十件しか受け付けられなくなる。**
容量の上限（下記15万件）には到底届かない。有料プランでも構造は同じで、
桁が増えるだけで頭打ちは残る。

**直し方**（作り直しではなく、3つとも安い）:
- 総件数を毎回数えない。集計値を1行のテーブルに持って、挿入時に加算する
- 中央値を送信のたびに計算しない。定期的に計算して保存したものを返す
- `avg_pt` / `glow_rate` に索引を張る

これで1送信あたりの読み取りが**件数に関係なく一定**になる。
`functions/api/stats.js` も同じ全件走査をしているので、あわせて直す。

### 容量の実測値（2026-08-19）

スキーマから同じ構造のSQLiteを作り、実データに近い行を入れて測った。

| 1実戦のCZ突入 | 1実戦あたり | 500MBに入る件数 |
|---|---|---|
| 10行 | 1,170 B | 約45万件 |
| 20行 | 1,955 B | 約27万件 |
| **40行（現実的）** | **3,520 B** | **約15万件** |
| 80行 | 6,651 B | 約8万件 |

容量の大半は `records` ではなく **`cz_points`**。CZ突入1回を1行に展開しているので、
実戦1件で30〜50行増える。

**いまのプラン（Workers 無料）の枠**:
- **1データベース 500MB**（アカウント合計は5GBだが、1DBの上限は500MB）
- 書き込み 100,000行/日 → 実戦にして1日2,400件。当面問題にならない
- 読み取り 5,000,000行/日 ← 上の問題で先に効く

人数に直すと、15万件は **週1回の人なら約3,000人の1年分**、
月2回なら約6,200人、週3回なら約1,000人。
上の読み取り問題さえ直せば、**数千人が1年使っても無料枠で足りる**。

有料（Workers Paid $5/月）にすると 1DB 10GB になり、同じ作りで300万件規模まで伸びる。

### 公開範囲の線引き（決定済み）

| | 公開 | 管理者のみ |
|---|---|---|
| 総件数・参加端末数・総ゲーム数 | ○ | |
| 規定pt分布（件数を必ず併記） | ○ | |
| 発光率・平均pt/CZ の中央値 | ○ | |
| 1実戦ずつの生データ | | ● |
| 端末ID別の件数 | | ● 端末の追跡になる |
| 設定申告の内訳と根拠 | | ● |
| 受信時刻の細かい分布 | | ● 生活パターンが読める |
| 弾いた異常値 | | ● |

設定申告を伏せる理由がいちばん大事。出すと「設定6のサンプルが3件」のような数字が
独り歩きして、サイトの信頼を最初に壊す。

**しきい値ルール**: 件数が一定未満のあいだは数字を出さず「集計中（あと◯件）」と出す。

---

## 判断済みで、いまは動かさないもの

- **会員制**: 2026-08-13 に方針転換した。下の「保存の会員制化」を参照。
  以前は「全員に登録を求める形」を想定して不採用にしていたが、
  数えるのは登録不要・保存だけ登録、という分け方なら費用と便益の置き場所が合う。
- **引き継ぎ機能は残す**（送信機能を外した版でも残置）。会員制ができるまでの橋渡しとして必要。
  **サーバー経由なので、Safariとホーム画面アプリの間でも記録を移せる**（実機で確認済み）。
- **機種定義のデータ化**: 保留。キャラ・ポイント配分・規定ptの刻みがJSに直書きだが、
  機種ごとに項目が違うので、当面は機種ごとに作るほうが早い

---

## 収益化（2026-08-13 の判断・当面やらない）

**結論**: いまはやらない。まずシェアして人が来るかを見る。
やるとしても買い切りではなく、**データが集まってからのフリーミアム**。

### 買い切りアプリにしない理由

- **コンセプトと噛み合わない**。価値はデータが集まることなので、
  入口を有料にすると「人が減る→データが集まらない→判別が当てにならない→
  価値が下がる」の逆回転が起きる。ネットワーク効果を自分で削ることになる
- カウンター単体なら売れるが、それは「みんなのスロット」の構想を捨てる話
- **費用が合わない**。App Store は Apple Developer Program が年99ドル（約1.5万円）。
  買い切りなのに費用は毎年。300〜500円だと回収に年数十本が要る。
  競合に無料アプリが多い狭い市場で、利用者0人からの出発
- Webのままライセンスキー方式にすると、決済・キー発行・検証・失効管理が要るうえ、
  **「登録もインストールも要りません」という一番の強みを捨てる**
- 副次的な利点として、ストアアプリにすると Safari とホーム画面アプリの
  データ分裂は解決する。ただしそれだけのために年1.5万円と審査対応は割に合わない

### 収益化するときの順番

1. まず人を集める（無料・登録不要のまま）
2. データが集まって、みんなの集計が実際に動き出す
3. そこで課金ポイントが生まれる。詳細な設定判別、機種横断の比較、
   履歴の無制限保存など、**データが集まって初めて価値が出るもの**を売る

いま課金しても売るものがカウンターしかない。3の段階なら他で手に入らないものになる。

### 広告を入れる場合の注意（入れるとしても人が来てから）

- **数える画面には置かない**。片手で連打する道具なので、誤タップで画面が飛ぶと
  何を数えていたか分からなくなる。置くなら取扱説明書・実戦履歴・トップページ
- **圏外で壊れないこと**。オフライン起動を売りにしているので、
  広告スクリプトが読めなくてもレイアウトが崩れない作りにする
- **AdSense より自分で選べるアフィリエイトのほうが向く**。自動配信だと
  情報商材のような広告が出て、「正直にデータを持ち寄る」というコンセプトを壊す
- **「PR」「広告」の明示が義務**（2023年10月〜のステマ規制／景品表示法）。
  書かないと不当表示になる
- **オンラインカジノの案件は扱わない**。日本では違法で、紹介行為が問題になりうる

---

## サイト側

- **トップページを本編に戻す**: `python3 src/top-mode.py full` のあとデプロイ。
  送信機能を戻すタイミングと合わせて判断する（本編には送信の記述がある）
- **本編の最後の見出し**が「一人の実戦は、たいてい一人で終わる。」になっている。
  締めが問題提起で終わるので読後感が重い。変えるなら
  「その一台ぶんを、置いていこう。」「あなたの1台が、サンプルになる。」など
- **ロゴの別案**: 「たくさんのメダルの中に一枚だけ色違い」も候補として残っている

---

## ホーム画面アイコンの作り直し（デザイン検討中・保留）

いまのアイコンは中の文字が「カバネリ／カウンター」のままで、機種名が
「カバネリ海門カウンター」に変わったのに追従していない。
赤緑青の3つの丸も、サイトのロゴから外した旧配色。

**決まったこと**:
- 方向は「マーク＋機種名」。ロゴマーク（積んだメダル）を入れる
- 背景は**白**（`#fdfdfd`）でサイトと合わせる。マークは黒 `#26262a` ＋ 青 `#2f7cf6`
- マークの横に「みんなのスロット」を入れる
- 「カバネリ」と「海門」は**同じ大きさ**（2行・34px）。1行にすると25pxまで落ちて逆効果

**未決**: 最終形をこの2行版で確定するか。
実寸60pxだと情報が3段（みんなのスロット／カバネリ／海門）になり、
ぱっと見の識別速度は「海門」だけを大きくした案に劣る。

**作り直しの手順**: `src/make-icons.py` を書き換えて Mac で実行する。
ヒラギノ明朝・角ゴシックを使うので **macOS でしか動かない**。Pillow が必要。

```bash
python3 -m pip install Pillow
python3 src/make-icons.py
```

書き出されるもの: `apple-touch-icon.png`(180) / `icon-192` / `icon-512` /
`icon-maskable-512` / `ogp.png`(1200x630)

**あわせて決めること**:
- iOSのホーム画面に出る名前がいま「**海門カウンター**」（`apple-mobile-web-app-title`）。
  アイコンに機種名が入るので重複する。変えるかどうか
- サイト側（`index.html`）のファビコンとアイコンは、まだカウンターのものを流用している。
  同時に差し替える

---

## 雑務

- **git履歴に残ったメールアドレス**: 過去のコミットに残っている。書き換えるか未決
- **Xでの告知**: カウンター単体（送信なし）でシェアする。まだ実施していない
- **`kabaneri/`（旧URL）**: いつ畳むか
